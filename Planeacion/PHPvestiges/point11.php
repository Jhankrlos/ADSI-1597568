<?php

/**
 * An array in PHP is actually an ordered map. A map is a type of data that
 *  associates values with keys. This type is optimized for several different
 *  uses; it can be used as an array, list (vector), associative table
 *  (hash table - an implementation of a map), dictionary, collection, stack,
 *  queue, and possibly more.
 */
$friends = (array('Juan','Esteban', 'Cristian'));
echo
$friends[0]; ?>
<br>
<?php
echo
$friends[1]; ?>
<br>
<?php
echo
$friends[2]; ?>

